defaultTasks 'compileLESS'

group = 'com.creaction.bi'
version = '1.0.0-SNAPSHOT'
description = 'Business Intelligence Analysis System Html Version'

repositories {
    mavenLocal()
    mavenCentral()
}

buildscript {

    repositories {
        mavenLocal()
        mavenCentral()
        maven {
            url 'https://github.com/skhome/maven-repository/raw/master'
        }
    }

    dependencies {
        classpath 'net.skhome:gradle-less-plugin:1.0.0'
    }

}

apply plugin: 'less'

less {
    ext.lessRoot = 'styles'
    sourceDir "$lessRoot"
    outputDir "$lessRoot"
    includes '**/*.less'
    excludes 'reset.less'
    force true
//    compress true
}
